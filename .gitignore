# Environment variables — NEVER commit these (including .example templates)!
.env
.env.local
.env.*
.env.migration
.env.production
.env.development
apps/api/.env
apps/web/.env.local
apps/*/.env
apps/*/.env.local
apps/*/.env.*
# Ignore ALL env files, including example/template files, in every directory.
**/.env.example
.env.example
*.dump
*.sql.gz
backup/
db-backups/

# Dependencies
node_modules/
.pnp
.pnp.js

# Build outputs
dist/
.next/
.open-next/
out/
build/
.codex-tools/

# Foundry
packages/contracts/out/
packages/contracts/cache/
packages/contracts/broadcast/

# OS
.DS_Store
Thumbs.db

# Logs
*.log
npm-debug.log*
pnpm-debug.log*

# Editor
.vscode/settings.json
.idea/

# Docs
docs/*
!docs/docs/
!docs/docs.json
!docs/favicon.svg
!docs/tokenomics.md
docs/site/
docs/.cache/

# Audit tooling & report (generated by the smart-contract-audit skill; not part of the app)
/smart-contract-audit/
/smart-contract-audit.skill
/DustSwapSweepRouter_Audit_Report.md

# Raw static-analysis output. Kept locally only: publishing unfiltered scanner
# findings for a live router hands attackers a pre-built map. The written audit
# reports in packages/contracts/audit/*.md stay public.
packages/contracts/audit/tool-output/

# AI / agent tooling. Local developer tooling, not application code, and the
# local settings files contain machine-specific absolute paths.
.claude/
.agents/
.codex-dev-logs/
.codex-remote-attachments/
.codex-run/
.codex-tools/
skills-lock.json
*.skill

# Build caches & bundler temp output
.wrangler/
*.tsbuildinfo

# One-off scratch, probe and codemod scripts. Useful locally, not production code.
scratch.*
test_date.js
test-wagmi-config.ts
fetch_abi.js
fix_themes.js
replace_admin_ui*.js
replace_admin_ui*.py
replace_theme.py
contract_info.json
v4quoter_abi.json
v4quoter_quotes.json
apps/web/test_contract.mjs
/test.ts

# PRIVATE USER DATA — never commit. Holds real wallet addresses and per-user
# volume for the Footprint Drop allowlist. Deploy it to the API host out-of-band
# (Railway volume / object storage); the service degrades gracefully without it.
apps/api/data/all_time_user_totals.json
apps/api/data/
